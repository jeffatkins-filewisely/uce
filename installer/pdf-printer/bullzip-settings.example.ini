; Bullzip PDF Printer — user profile (legacy %APPDATA%\Bullzip\PDF Printer\settings.ini).
; Bullzip 11+ / 2026: prefer system-wide C:\ProgramData\PDF Writer\<queue>\global.ini
; (FileWisely install.ps1 and repair-bullzip-silent.ps1 write that for you — silent save to Incoming).

[Settings]
; Per-user override (optional). ProgramData global.ini takes precedence for many builds.

[PDF Printer]
; Same section as global.ini if your build reads this path:
; Output=C:\FileWisely\Incoming\<date>_<time>_<docname>.pdf
; ShowSaveAS=never
; ShowSettings=never
; ShowPDF=no
